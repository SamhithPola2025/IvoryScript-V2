// arithmetic.ivc
// Exercises integer literals and the four arithmetic operators,
// including parenthesized sub-expressions to override precedence.

int sum = 1 + 2 + 3;
int product = (2 * 3) + 4;
int mixed = (10 - 2) / 4;

print(sum);
print(product);
print(mixed);
